# 1. The universal fiber-collapse construction

Let $\pi:X\longrightarrow Y$ be any map of sets.

For $y\in Y$, write

```math
X_y=\pi^{-1}(y).
```

Define the algebraic pushforward

```math
\Sigma_\pi^0:k^{(X)}\longrightarrow k^{(Y)}
```

by

```math
(\Sigma_\pi^0f)(y)=
\sum_{x\in X_y}f(x).
```

The sum is always finite because $f$ has finite support.

Its kernel is

```math
\mathfrak D_\pi=
\ker\Sigma_\pi^0=
\bigoplus_{y\in Y}
\left\lbrace f\in k^{(X_y)}: \sum_{x\in X_y}f(x)=0 \right\rbrace.
```

There is an exact sequence

```math
0\longrightarrow
\mathfrak D_\pi
\longrightarrow
k^{(X)}
\xrightarrow{\Sigma_\pi^0}
k^{(\pi(X))}
\longrightarrow0.
```

Every defect is generated by differences

```math
\delta_x-\delta_{x'}
\qquad
\bigl(\pi(x)=\pi(x')\bigr).
```

The construction contains the route-collapse ideals used above as special cases.

# 2. The kernel-pair groupoid

Associated to $\pi$ is the equivalence-relation groupoid

```math
\mathcal G_\pi=
X\times_YX=
\{(x,x'):\pi(x)=\pi(x')\}.
```

Its operations are

```math
(x,x')^{-1}=(x',x),
```

and

```math
(x,x')(x',x'')=(x,x'').
```

The unit at $x$ is $(x,x)$.

Inside one fiber $X_y$, this is the full pair groupoid

```math
X_y\times X_y.
```

Its algebraic matrix units act on $k^{(X)}$ by

```math
E_{x,x'}\delta_{x'}=\delta_x.
```

It follows that

```math
(E_{x,x'}-E_{x',x'})\delta_{x'}=
\delta_x-\delta_{x'}.
```

The groupoid records the collapsed elements and every coherent replacement operation within each collapse fiber.

For a discrete equivalence relation, the regular groupoid representation gives matrix blocks over equivalence classes; under locally compact Hausdorff étale hypotheses, the usual convolution algebra and full/reduced groupoid $C^\ast$-completions are available.

# 3. Hilbert-space regularity of a general collapse

On algebraic finite-support vectors, define

```math
\Sigma_\pi^0\delta_x=\delta_{\pi(x)}
```

as an operator

```math
\ell^2(X)\supset k^{(X)}
\longrightarrow
\ell^2(Y).
```

Exactly the same trichotomy derived for endpoint collapse holds. If

```math
M=\sup_{y\in Y}|X_y|<\infty,
```

then $\Sigma_\pi^0$ is bounded and

```math
|\Sigma_\pi|=\sqrt M.
```

If every $X_y$ is finite but the fiber sizes are unbounded, then $\Sigma_\pi^0$ is closable with unbounded closure. If some $X_y$ is infinite, then $\Sigma_\pi^0$ is nonclosable.

Indeed, choosing distinct $x_1,\ldots,x_N\in X_y$,

```math
f_N=\frac1N\sum_{j=1}^N\delta_{x_j}
```

satisfies

```math
|f_N|_2=N^{-1/2}\to0
```

but

```math
\Sigma_\pi^0f_N=\delta_y.
```

The analytic behavior depends only on the collapse-fiber cardinalities.

The same construction applies to finite route fibers and Martin-boundary fibers.

# 4. Finite path collapse as a kernel pair

Take $X_{\mathrm{fin}}=\mathsf P(G)$.

and

```math
Y_{\mathrm{fin}}=
\{(u,v):u\le v\}.
```

The finite endpoint-collapse map is

```math
\pi_{\mathrm{fin}}(p)=
\bigl(s(p),r(p)\bigr).
```

It follows that

```math
\Sigma_{\pi_{\mathrm{fin}}}^0(p)=
\varepsilon_{r(p),s(p)},
```

the unique thin reachability arrow with the same endpoints.

After linearization, this is exactly

```math
Q:A(G)\longrightarrow B(P_G).
```

Consequently,

```math
\mathfrak D_{\pi_{\mathrm{fin}}}=
\ker Q=
I.
```

The finite kernel-pair groupoid is

```math
\mathcal G_{\mathrm{fin}}=
\{
(p,q):
s(p)=s(q),\;
r(p)=r(q)
\}.
```

Its arrows are replacements of one actual route by another parallel route.

The bisimplicial object exposes two independent compositions.

## Horizontal composition

Horizontal composition is ordinary path concatenation,

```math
q p.
```

## Vertical composition

Vertical composition is route replacement,

```math
(p,q)(q,r)=(p,r).
```

They are not the same multiplication.

The double structure carries both route and factorization directions:

```math
\text{horizontal: communication composition},
\qquad
\text{vertical: route identification}.
```

# 5. Finite route replacements propagate to infinite histories

Let $p,q:u\to v$ be finite paths with the same endpoints.

Let $\zeta=v\to\cdots$ be an infinite tail beginning at $v$.

It follows that

```math
p\zeta
\qquad\text{and}\qquad
q\zeta
```

are two infinite histories beginning at $u$.

They eventually have exactly the same tail and hence the same node Martin cluster set and, when regular, the same node Martin point.

Thus every finite route replacement produces a boundary replacement:

```math
(p,q;\zeta)
\longmapsto
(p\zeta,q\zeta).
```

In the standard graph boundary-path groupoid, sets of the form

```math
Z(p,q)=
\{
(p\zeta,\ |p|-|q|,\ q\zeta)
\}
```

are the basic prefix-replacement bisections. Their multiplication and inversion encode composition of finite prefix replacements.

# 6. Boundary creator representation

Define $\mathcal H_\partial$ by

```math
\mathcal H_\partial=
\ell^2(X_{\mathrm{pre}})
```

be the Hilbert space with an orthogonal basis indexed by boundary histories in the chosen prefix-boundary space.

A finite path $p$ defines a partial creator

```math
L_p^\partial\delta_\zeta=
\delta_{p\zeta}
```

when $\zeta$ begins at $r(p)$.

For parallel paths $p,q$, define

```math
U_{p,q}=
L_p^\partial(L_q^\partial)^\ast.
```

It acts by

```math
U_{p,q}\delta_{q\zeta}=
\delta_{p\zeta}.
```

For paths $p,q,r$ with the same terminal node,

```math
U_{p,q}^\ast=U_{q,p},
```

and

```math
U_{p,q}U_{q,r}=U_{p,r}.
```

These are matrix-unit relations inside a boundary-tail fiber.

The finite route defect acts as

```math
(L_p^\partial-L_q^\partial)\delta_\zeta=
\delta_{p\zeta}-\delta_{q\zeta}.
```

The propagated finite route ideal is exactly the algebraic span of eventual-tail defects:

```math
I,k^{(X_{\mathrm{pre}})}=
\mathrm{span}
\{
\delta_\xi-\delta_\eta:
\xi\sim_{\mathrm{tail}}\eta
\}.
```

The propagated ideal gives a direct finite-to-infinite bridge.

# 7. Two nested boundary equivalence relations

Fix a source $u$, a weight $t$, and the set $X_u^{\mathrm{reg}}$ of $t$-Martin-regular histories beginning at $u$.

There are two relevant equivalence relations. Define eventual-tail equivalence by $\xi\sim_{\mathrm{tail}}\eta$ when

```math
\xi=p\zeta,
\qquad
\eta=q\zeta,
```

where $p,q:u\to v$ are parallel finite routes and $\zeta$ is a common infinite tail beginning at $v$. This relation is generated by propagated finite route replacements. Define Martin equivalence by $\xi\sim_t^M\eta$ when $h_\xi=h_\eta$.

Because eventual common tails have identical node Martin limits,

```math
\sim_{\mathrm{tail}}
\ \subseteq
\sim_t^M.
```

There is a two-stage compression:

```math
X_u^{\mathrm{reg}}
\longrightarrow
X_u^{\mathrm{reg}}/\!\sim_{\mathrm{tail}}
\longrightarrow
X_u^{\mathrm{reg}}/\!\sim_t^M.
```

The first quotient removes finite-prefix route choices.

The second identifies genuinely different ends with the same normalized Green asymptotics.

# 8. Tail ends inside a Martin fiber

Let $\pi_t:X_u^{\mathrm{reg}}\to Y_{t,u}$ be the node Martin map,

```math
\pi_t(\xi)=h_\xi.
```

For $h\in Y_{t,u}$, define its history fiber

```math
F_h=\pi_t^{-1}(h).
```

Now quotient this fiber by eventual-tail equivalence:

```math
\overline F_h=
F_h/\!\sim_{\mathrm{tail}}.
```

An element of $\overline F_h$ is a genuine tail end, not one history, with all finite-prefix alternatives already identified.

```math
|\overline F_h|
```

measures how many distinct tail ends are collapsed into the single node Martin direction $h$.

The quotient separates finite route multiplicity from asymptotic end multiplicity.

Prefix multiplicity occurs when several histories differ only by finite routes and eventually share a tail. End multiplicity occurs when genuinely different tails have the same Martin shadow.

# 9. Exact defect sequence

Define the algebraic tail-defect module

```math
\mathfrak D_{\mathrm{tail}}=
\mathrm{span}
\{
\delta_\xi-\delta_\eta:
\xi\sim_{\mathrm{tail}}\eta
\}.
```

Define the Martin-defect module

```math
\mathfrak D_M=
\mathrm{span}
\{
\delta_\xi-\delta_\eta:
\xi\sim_t^M\eta
\}.
```

It follows that

```math
\mathfrak D_{\mathrm{tail}}
\subseteq
\mathfrak D_M.
```

For a set $S$, write

```math
\widetilde{k}^{(S)}=
\left\lbrace f\in k^{(S)}: \sum_{s\in S}f(s)=0 \right\rbrace.
```

There is a canonical exact sequence

```math
0
\longrightarrow
\mathfrak D_{\mathrm{tail}}
\longrightarrow
\mathfrak D_M
\longrightarrow
\bigoplus_{h\in Y_{t,u}}
\widetilde{k}^{(\overline F_h)}
\longrightarrow
0.
```

## Quotient computation

Inside a Martin fiber $F_h$:

1. $\mathfrak D_M$ permits all zero-total differences among histories in $F_h$;
2. $\mathfrak D_{\mathrm{tail}}$ kills differences internal to each tail class;
3. after quotienting, each tail class becomes one basis element;
4. the remaining relation is that the total coefficient over all tail classes is zero.

```math
\mathfrak D_M/\mathfrak D_{\mathrm{tail}}
\cong
\bigoplus_h
\widetilde{k}^{(\overline F_h)}.
```

The quotient is the asymptotic mixing defect.

It contains precisely those Martin identifications that cannot be generated by finite route replacements.

# 10. Finite-dimensional interpretation

If one Martin point $h$ contains exactly $r$ distinct tail classes, then

```math
\dim
\widetilde{k}^{(\overline F_h)}=
r-1.
```

* $r=1$: all boundary collapse is explained by finite route ambiguity;
* $r=2$: one independent asymptotic identification between two ends;
* $r>2$: an $(r-1)$-dimensional space of asymptotic end differences.

For infinite $r$, the algebraic module remains well defined, although a Hilbert collapse to one equal-end direction becomes nonclosable.

# 11. Three examples

## Unique-route tree

Every history has its own node Martin point.

```math
F_h=\{\xi\},
\qquad
\overline F_h=\{\xi\}.
```

Equivalently,

```math
\mathfrak D_M=
\mathfrak D_{\mathrm{tail}}=
0.
```

Nothing is collapsed, either finitely or asymptotically.

## Diamond followed by one tail

There are two histories

```math
p\zeta,
\qquad
q\zeta.
```

They differ by a finite diamond but share the same eventual tail.

```math
|F_h|=2,
```

but

```math
|\overline F_h|=1.
```

Thus the two defects satisfy

```math
\mathfrak D_M\ne0,
\qquad
\mathfrak D_M/\mathfrak D_{\mathrm{tail}}=0.
```

All boundary multiplicity is inherited from the finite route defect

```math
p-q.
```

There is no genuinely new asymptotic mixing.

## Complete layered mixing

Every infinite symbolic history has the same node Martin point, but two generic histories are not eventually tail-equivalent.

Therefore one Martin fiber contains a vast collection of distinct tail classes:

```math
\overline F_h
\cong
X_{\mathrm{pre}}/\!\sim_{\mathrm{tail}}.
```

The reduced coefficient space $\widetilde{k}^{(\overline F_h)}$ is therefore large.

Here the Martin collapse arises not from any finite diamond or finite reconvergence, but from asymptotic mixing of endpoint Green ratios.

# 12. The asymptotic mixing nerve

For each Martin point $h$, form the chaotic nerve

```math
E_{\mathrm{mix}}(h)=
E(\overline F_h).
```

Its simplices are

```math
E_{\mathrm{mix}}(h)_n=
\overline F_h^{\,n+1}.
```

The vertices are the distinct tail ends with Martin shadow $h$. Edges encode asymptotic identifications between two ends, triangles encode coherence among three identifications, and higher simplices encode the corresponding higher coherence of the Martin collapse.

Its degree-one boundary is

```math
d_1(\tau_0,\tau_1)=
\tau_1-\tau_0.
```

Equivalently,

```math
\mathrm{im}d_1=
\widetilde{k}^{(\overline F_h)}.
```

The exact defect sequence can be rewritten as

```math
\text{finite route defects}
\longrightarrow
\text{all Martin defects}
\longrightarrow
\text{asymptotic end-mixing resolution}.
```

Again the nerve is contractible when nonempty, but its marked vertices retain the distinct ends being collapsed.

# 13. Two-stage boundary resolution

The full collapse of histories within a Martin fiber admits a two-stage resolution. Within each tail class $T\subseteq F_h$, the first stage uses $E(T)$ to resolve histories related by finite route replacements. The second stage collapses the resulting tail classes using $E(\overline F_h)$.

The boundary analogue of the earlier route-factorization bicomplex is

```math
\text{history}
\longrightarrow
\text{tail end}
\longrightarrow
\text{Martin direction}.
```

The first differential is generated by the original finite route ideal $I$.

The second differential records genuinely asymptotic identifications that have no finite route witness.

# 14. Operator-algebraic meaning

The boundary replacement operators

```math
U_{p,q}=L_p^\partial(L_q^\partial)^\ast
```

generate the algebra of eventual-tail replacements.

The algebra is the operator-algebraic image of the finite kernel-pair groupoid.

The larger Martin relation

```math
\mathcal G_M=
\{
(\xi,\eta):
\xi\sim_t^M\eta
\}
```

contains that tail groupoid as a subgroupoid.

However, the following restriction remains:

```math
\mathcal G_M
\text{ is not automatically étale or locally compact}.
```

The standard graph tail groupoid has a canonical topology built from finite-prefix bisections. The Martin relation may have large nondiscrete fibers or may fail to be closed.

Therefore, under the bounded system definition alone, the canonical primary object is

```math
\left(
\mathcal G_{\mathrm{tail}}
\subseteq
\mathcal G_M,\;
\mathfrak D_{\mathrm{tail}}
\subseteq
\mathfrak D_M
\right)
```

at the set-theoretic and algebraic level.

A standard groupoid convolution algebra or $C^\ast$-completion requires additional topological hypotheses such as local compactness, a suitable Haar system, or étaleness, after which standard étale groupoid convolution and completion theory applies.

# 15. Primacy of the algebraic module

Suppose a Martin fiber, or even one tail class, is infinite.

Then the algebraic zero-sum defect vectors are dense in its entire $\ell^2$ fiber.

Consequently, taking Hilbert closures too early can erase the distinction between:

* finite-prefix defects;
* asymptotic mixing defects.

The quotient

```math
\overline{\mathfrak D_M}/\overline{\mathfrak D_{\mathrm{tail}}}
```

can become zero even while

```math
\mathfrak D_M/\mathfrak D_{\mathrm{tail}}
```

is enormous.

The algebraic defect filtration is more informative than its Hilbert closure.

The same noncommutation between route collapse and norm closure appears at the boundary.
